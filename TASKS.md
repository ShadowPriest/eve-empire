# Задачи EVE Empire

Рабочая очередь. ARCHITECTURE.md описывает, КАК всё устроено; здесь — что ещё сделать.

## Фоновые операции

В приложении сейчас нет ни одной фоновой задачи: все данные собираются только в момент
захода на страницу. Это главное ограничение всей статистики.

- [ ] **Фоновый сбор добычи** — леджер `/characters/{id}/mining/` и цены Житы за день
      складывать в `mining_ledger` **раз в 15 минут** И **при открытии страницы**
      (сейчас работает только второе). Зачем: окно ESI ~30 дней и конечно, а без
      регулярного сбора история рвётся, если месяц не заходить в кабинет. Кэш леджера
      у ESI 600 с, так что 15 минут — это ровно один свежий ответ на цикл, лишнего
      трафика нет.
- [ ] Каркас планировщика: одна горутина с тикером в `cmd/server/main.go`, список задач
      с интервалами, аккуратная остановка. Первым потребителем идёт добыча.
- [ ] Кандидаты в фоновые задачи после добычи:
      - телеметрия флота (`fleet_state` → `fleet_events`) — сейчас пишется, только пока
        открыта страница флота, слепые зоны по сменам корабля;
      - снимки имущества для анализа лута (диффы по типам);
      - лунные обсерверы: отдают НАКОПЛЕННУЮ сумму без дат, динамика возможна только
        собственными снимками счётчика.

## Добыча

- [x] Страница `/mining`: разрезы ISK / объём / количество, цена Житы за день добычи,
      своя история в `mining_ledger`, панель лун.
- [x] Клик по столбцу графика фильтрует списки по этому дню.
- [ ] Своды по неделям/месяцам, сравнение периодов.
- [ ] Учитывать сжатую руду и переработку: сейчас считается только сырая добыча.

## Таблица руд (`/tools/ore`) и рынок (`/tools/market`)

- [x] Своя копия ore.cerlestes.de: четыре вкладки (руда, лёд, лунная, газ), состав из
      `type_materials`, цены — перцентили по живому ордербуку Житы, вердикт
      «перерабатывать или продавать» на единицу товара.
- [x] Вотчлист товаров: PLEX (глобальный рынок 19000001), экстрактор, большой инжектор,
      топливные блоки — ордербук + дневная история за 14/90/365 дней.
- [x] Выход переработки по навыкам выбранного альта (`?ch=`): общие 3385/3389,
      профильный навык руды (атрибут 790) и импланты (атрибут 379), база станции —
      поле. Считается отдельно по каждой руде. У оригинала этого нет вовсе.
- [x] Окно настроек переработки: кто перерабатывает, где (НПС / Athanor / Tatara),
      риг (Reprocessing Monitor I/II) и пространство (хай/лоу/нули+ВХ). Все множители
      читаются из SDE, база пересчитывается вживую; настройка хранится в `app_settings`.
- [x] Свой набор настроек на руду, лёд и лунную руду (возят их в разные места).
      ПРОВЕРЕНО: покатегорийных множителей в игре нет — 2444/2445/2448 есть только у
      сервисного модуля и там все три по 0.50, у ригов только общий 717.
- [ ] Копить снимки ордербука в своей таблице, чтобы появились графики истории цен
      по рудам (у ESI дневная история есть только по типам, а перцентилей в ней нет).
- [ ] Выбор региона: сейчас жёстко The Forge (владелец так и просил), но
      `esi.RegionOrderBook` уже принимает любой регион.
- [ ] Газ: колонка «декомпрессия» (цена компресснутого × выход) — у оригинала есть.

## Модули из «Следующих шагов»

- [x] **Планировщик навыков** (`/tools/skill-planner`): импорт плана в игровом формате,
      хранилище планов (`skill_plans`), расчёт времени по рангу и паре характеристик,
      пересборка порядка без нарушения пререквизитов, подбор ремапа, оценка имплантов
      и церебрального ускорителя, экспорт обратно в игру. Алгоритмы — `internal/skillplan`.
- [ ] Планировщик: добавление навыков из каталога кнопками (`.skbtn` с `data-skill` уже
      стоят на странице «Навыки»), сейчас план вводится только вставкой текста.
- [x] **Расчёт производства** (`/tools/build`): поиск чертежа по имени продукта или
      чертежа, материалы по ME/прогонам, цены Житы по ордербуку, сбор за установку работы
      (EIV × индекс системы + налог + SCC), сравнение с ценой продукта, ISK/час.
- [x] Расчёт производства: компоненты РЕКУРСИВНО — «сделать или купить» по каждому узлу,
      три режима (покупать всё / считать выгоду / делать всё), поимённые решения в ссылке,
      целые прогоны с излишком, отдельный сбор на каждую суб-работу.
- [x] Расчёт производства: бонусы корпуса структуры (материалы/время) из SDE, сверка с
      клиентом на Hydrogen Fuel Block — материалы, объём, EIV, сбор и срок сошлись.
- [ ] Расчёт производства: риги структуры (материалы и время по группам предметов),
      стоимость инвента для T2 (шансы, дата-коры, ME/TE полученной копии), свои ME/TE по
      каждому дочернему чертежу вместо общих `cme` и TE страницы.
- [ ] **Планировщик и менеджмент производства** (пункты меню — заглушки): очереди работ по
      альтам поверх расчёта, слежение за незанятыми линиями.
- [ ] Свод по маркету: где ордера перебиты, что близко к истечению.
- [ ] Страница флота: решить, что с ней делать дальше (отложено владельцем).

## Почта и структуры

- [x] **Почта** (`/characters/{id}/mail`): папки-метки, рассылки, список по 50 с
      пагинацией вглубь (вся история, не 30 дней), чтение письма с игровой разметкой.
- [x] **Структуры** (`/structures`): топливо/состояние/сервисы/окно реинфорса по всем
      корпорациям (роль Station Manager перебором альтов) + события атак и топлива из
      личных уведомлений (единственный источник атак в ESI, дубли альтов склеены).
- [x] Почта: письма модалкой (`/api/mail/{char}/{mail}`), кликабельные рассылки
      (скан ленты — фильтра по рассылке в ESI нет), вкладка «Уведомления» (вторая
      вкладка игровой почты = `/notifications/`), красный счётчик непрочитанных на
      портретах в сайдбаре.
- [ ] Почта: отметка прочитанным и отправка писем (PUT/POST в ESI есть — почта входит
      в короткий список того, куда ESI вообще умеет писать).
- [ ] Уведомления: дополнять словарь `notifTypesRu` по мере появления сырых типов
      в ленте (фолбэк показывает английский тип — сразу видно, чего не хватает).
- [ ] Структуры: алерт в «Требует внимания» на сводке планет или отдельный маркер в
      шапке, когда структура в реинфорсе или топлива меньше двух суток (пока это видно
      только при заходе на /structures; фоновых задач в приложении нет).

## Хвосты

- [ ] Пагинация кошелька (588 строк), сортировка имущества, пустоватая корп-инфо,
      favicon, замена native prompt/alert.
- [x] Деплой: сборка образа arm64 без Docker (`deploy/build-arm64.ps1` + `cmd/mkimage`),
      инструкция по RB5009 — `deploy/README.md`.
- [ ] Деплой, хвосты: прогнать образ на живом роутере, бэкапы SQLite (WAL!), автозапуск
      и обновление одним скриптом вместо ручного `/container/add`.
- [ ] Безопасность: перегенерировать Client Secret (засвечен в чате), `git init`
      (репозитория до сих пор нет).
